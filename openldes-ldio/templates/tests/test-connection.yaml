apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openldes-ldio.fullname" . }}-test-connection"
  labels:
    {{- include "openldes-ldio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openldes-ldio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
