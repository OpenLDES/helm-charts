apiVersion: v1
kind: Service
metadata:
  name: {{ include "openldes-ldio.fullname" . }}
  labels:
    {{- include "openldes-ldio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openldes-ldio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ldio
