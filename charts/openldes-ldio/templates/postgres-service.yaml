{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "openldes-ldio.fullname" . }}-postgres
  labels:
    {{- include "openldes-ldio.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "openldes-ldio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
{{- end }}

