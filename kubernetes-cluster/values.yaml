# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: openldes/ldes-server
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: latest

# Additional environment variables for the LDES server container
env:
  MANAGEMENT_TRACING_ENABLED: "false"
  # Set SIS_DATA to a writable directory if you want to use Apache Spatial Information System (SIS) (if you want to make use of by-location fragmentation)
  # This will extend the startup time of the LDES server significantly
  # SIS_DATA: "/tmp"

# -- LDES Server application configuration
# Reference: https://openldes.github.io/LDESServer/4.0.0/how-to-run.html
config:
  # Spring API docs & Swagger UI
  springdoc:
    apiDocsPath:
    swaggerUiPath:

  server:
    # Bind address for the server
    address:

  ldesServer:
    # Public hostname used in generated URIs
    hostName: http://localhost:8080
    # Use relative URLs in responses
    useRelativeUrl:
    # Port configuration
    fetch:
      port:
    ingest:
      port:
    admin:
      port:
    # Cron expression for fragmentation
    fragmentationCron:
    # ISO 8601 duration for compaction
    compactionDuration:
    # Cron expression for maintenance
    maintenanceCron:
    # Formatting
    formatting:
      prefixes: { }

  # Cache control
  rest:
    maxAge:
    maxAgeImmutable:

  # PostgreSQL connection settings. Use these settings if you want to connect to a Postgres database that exists outside of this application.
  # This should be used in a production environment, with "postgres.enabled" = false.
  # When you set "postgres.enabled" to true, these values will be ignored: the url, username and password will be set to these of the Postgres that will be created
  spring:
    datasource:
      url:
      username:
      password:

  # -- Additional configuration to merge into application.yml
  extraConfig: { }

# Set the replicaCount to 0 or 1 (multiple instances is not yet supported)
replicaCount: 1

# Postgres database to be used by the LDES server. Don't use this in production, but it is useful for testing and development.
# If you want to use an external database, you can set enabled to false and provide the connection settings in config.spring.datasource.
postgres:
  # Don't enable the Postgres database in a production environment!
  enabled: true
  image:
    repository: postgres
    pullPolicy: IfNotPresent
    tag: latest
  # Database name to create
  database: ldes
  # Password for the admin user (auto-generated if not set)
  password: ""
  # PVC storage size
  storage: 1Gi
  # Storage class name (uses default if not set)
  # storageClassName: ""

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: [ ]
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created.
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account.
  annotations: { }
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: { }
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: { }

podSecurityContext: { }
# fsGroup: 2000

securityContext: { }
# TODO
#  readOnlyRootFilesystem: true
#  runAsNonRoot: true
#  runAsUser: 2000
#  seccompProfile:
#    type: RuntimeDefault

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # The settings for the ports can be found under config.ldesServer.fetch, config.ldesServer.ingest and config.ldesServer.admin

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: openldes.local
      paths:
        - path: /
          pathType: Prefix
  tls: [ ]
    # - secretName: openldes-server-tls
  #   hosts:
  #     - openldes-server.local

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
httpRoute:
  # HTTPRoute enabled.
  enabled: false
  # HTTPRoute annotations.
  annotations: { }
  # Which Gateways this Route is attached to.
  parentRefs:
    - name: gateway
      sectionName: http
      # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
    - openldes-server.local
  # List of rules and filters applied.
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /headers
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2

resources:
  limits:
    cpu: 500m
    memory: 2Gi
  requests:
    cpu: 250m
    memory: 256Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /actuator/health
    port: fetch
readinessProbe:
  httpGet:
    path: /actuator/health
    port: fetch

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: [ ]
  # - name: foo
  #   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: [ ]
  # - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: { }

tolerations: [ ]

affinity: { }
